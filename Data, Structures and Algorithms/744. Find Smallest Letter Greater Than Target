class Solution {
    public char nextGreatestLetter(char[] letters, char target) {
        for(char ch : letters){
            if(ch > target ){
                return ch; //if we found the ch we return it immedieately
            }
        }
        return letters[0];
    }
}
