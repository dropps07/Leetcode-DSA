class Solution {
    public int findMaxConsecutiveOnes(int[] nums) {
      int c=0;
      int maxC=0;
      for(int x: nums){
        if(x==1){
            c++;
            maxC = Math.max(maxC, c);
        }else{
            c=0; //reset streak
        }
      }  
      return maxC;
    }
}
